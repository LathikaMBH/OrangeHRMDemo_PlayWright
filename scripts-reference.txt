Add these scripts to your package.json (works with your original structure):

{
  "scripts": {
    "setup": "./setup.sh",
    
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:debug": "npx playwright test --debug",
    "test:ui": "npx playwright test --ui",
    
    "test:dev": "TEST_ENV=dev npx playwright test",
    "test:staging": "TEST_ENV=staging npx playwright test",
    "test:prod": "TEST_ENV=prod npx playwright test",
    
    "test:chrome": "npx playwright test --project=chromium",
    "test:firefox": "npx playwright test --project=firefox",
    "test:safari": "npx playwright test --project=webkit",
    "test:mobile": "npx playwright test --project=mobile-chrome --project=mobile-safari",
    
    "test:smoke": "npx playwright test --grep @smoke",
    "test:regression": "npx playwright test --grep @regression",
    "test:api": "npx playwright test tests/api/",
    "test:visual": "npx playwright test --grep @visual",
    
    "report": "npx playwright show-report",
    "report:open": "REPORT_OPEN=true npx playwright show-report",
    
    "codegen": "npx playwright codegen",
    "install-browsers": "npx playwright install",
    "install-deps": "npx playwright install-deps",
    "update-snapshots": "npx playwright test --update-snapshots",
    
    "ai:generate": "node ai-agent/src/agent.js --generate",
    "ai:analyze": "node ai-agent/src/agent.js --analyze",
    "ai:test": "node dist/ai-agent/src/agent.js",
    
    "build": "tsc",
    "clean": "rm -rf test-results playwright-report reports screenshots videos",
    
    "lint": "eslint . --ext .ts,.js",
    "lint:fix": "eslint . --ext .ts,.js --fix",
    "format": "prettier --write ."
  }
}

IMPORTANT NOTES:
- Your original ai-agent/src/agent.ts structure is preserved
- All AI scripts reference your original file locations
- Environment configs work with your existing structure
- dist/generators/ matches your original build output
- All original directories are maintained exactly as they were
